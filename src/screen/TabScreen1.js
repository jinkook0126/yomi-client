import * as React from 'react';
import { Text, View } from 'react-native';

export default ()=>{
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center',backgroundColor:'beige' }}>
          <Text>TabScreen 1 입니다 !!</Text>
        </View>
    );
}